apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: hive-gateway
  namespace: trino
spec:
  selector:
    istio: ingressgateway
  servers:
    - port:
        number: 9083
        name: tcp-thrift
        protocol: TCP
      hosts:
        - '*'
